<h1>編集する</h1>
  <div class="form">
    <div class=form-input>
      <%= form_with(model: @body_record, local: true) do |f| %>
      <p>日付を選択してください</p>
        <%= f.date_field :date %>
      <p>体重を入力してください（例: 60.2）</p>
        <%= f.text_field :body_weight %>
      <p>体脂肪を入力してください（例: 18.6）</p>
        <%= f.text_field :fat %>
      <p>体調を選択してください</p>
        <%= f.text_field :todays_condition %>

     <p><%= f.submit "保存" %></p>
    </div>
      <% end %>
  </div>